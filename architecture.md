# 📊 Expense Tracker v2.0 - 系统架构与数据流图

---

## 一、系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                     展示层 (Presentation)                        │
│  ┌──────────┐   ┌────────────┐   ┌──────────────┐              │
│  │  Chrome   │   │  Vite :5173 │   │  Vue SPA     │              │
│  │  浏览器   │──▶│  HMR+Proxy │──▶│  Client App  │              │
│  └──────────┘   └────────────┘   └──────────────┘              │
├─────────────────────────────────────────────────────────────────┤
│                     代理层 (API Proxy)                           │
│              Vite dev server: /api → localhost:8001             │
├─────────────────────────────────────────────────────────────────┤
│                     前端业务层 (Frontend)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ Dashboard.vue │  │Transactions  │  │   Statistics.vue     │  │
│  │ 统计卡片+ECharts│  │ .vue CRUD表单 │  │ 饼图/柱状图/折线图    │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐                            │
│  │  Login.vue   │  │ Register.vue │                            │
│  │  登录表单     │  │  注册表单     │                            │
│  └──────────────┘  └──────────────┘                            │
├─────────────────────────────────────────────────────────────────┤
│                     后端服务层 (Backend API :8001)               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    FastAPI + uvicorn                      │   │
│  ├─────────────┬───────────────┬────────────────────────────┤   │
│  │ Auth Router │ Transactions  │    Statistics Router        │   │
│  │             │    Router     │                            │   │
│  │ POST regis-│ POST create   │ GET monthlyStats           │   │
│  │ ter(409)   │ GET list(?)   │ GET categoryBreakdown      │   │
│  │ POST login │ GET detail    │ GET trend                  │   │
│  │ GET /me    │ PUT update    │                            │   │
│  │            │ DELETE remove │                            │   │
│  └─────────────┴───────────────┴────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│                   ┌──────────────┐                              │
│                   │  aiosqlite    │                              │
│                   │ async driver │                              │
│                   └──────┬───────┘                              │
├────────────────────────────┼───────────────────────────────────┤
│                     基础设施层                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌──────────┐ │
│  │ SQLite DB  │  │  bcrypt    │  │ python-jose│  │ Python3.8│ │
│  │ expenses.db│  │ 哈希存储   │  │ JWT签发验证│  │ uvicorn  │ │
│  └────────────┘  └────────────┘  └────────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、数据流图 - 完整业务流程

### 2.1 用户注册 → 登录 → 记账 全流程

```
                                    ┌─────────────────┐
                                    │     浏览器       │
                                    │  localhost:5173  │
                                    └────────┬────────┘
                                             │
              ┌──────────────────────────────┼──────────────────────────────────────────┐
              │                                                              │
              ▼                                                              ▼
    ┌───────────────────┐                                        ┌───────────────────┐
    │  Step ① 首页访问   │                                        │  Step ③ 路由守卫   │
    │                   │                                        │                   │
    │  GET /            ├────────────────────────────────────────►│  requiresAuth:     │
    │  index.html       │                                        │  token exist?      │
    │  (SPA)            │                                        │  NO → redirect     │
    └────────┬──────────┘                                        └────────┬────────┘
             │                                                            │
             ▼                                                            ▼
    ┌───────────────────┐                                        ┌───────────────────┐
    │  Step ② 未登录页   │                                        │  Step ④ 跳转登录页 │
    │                   │                                        │                   │
    │  Redirect /login  │◄───────────────────────────────────────│  router.push(/login) │
    │  Login.vue        │                                        │                   │
    └────────┬──────────┘                                        └────────┬────────┘
             │
             ▼
    ┌───────────────────┐         ┌────────────────────────────────────┐
    │  Step ⑤ 用户操作   │         │  Step ⑥ Axios Interceptor          │
    │                   │         │                                    │
    │  填写用户名/密码   ├─────────┤  config.headers.Authorization      │
    │  点击"登录"        │         │  = `Bearer ${localStorage.token}`   │
    └────────┬──────────┘         └────────────────────────────────────┘
             │
             ▼
    ┌───────────────────┐         ┌────────────────────────────────────┐
    │  Step ⑦ HTTP 请求  │         │  Step ⑧ FastAPI Auth Middleware     │
    │                   │         │                                    │
    │  POST /api/auth/  │────────►│  oauth2_scheme(token)               │
    │  login            │         │  jwt.decode(token, SECRET_KEY)       │
    │                   │         └────────────────────────────────────┘
    │  User: v2test123  │         │                                    │
    │  Pass: password   │         ▼                                    │
    └────────┬──────────┘  ┌───────────────────┐                       │
             │             │  Step ⑨ 数据库查询  │                       │
             │             │                   │                       │
             │             │  SELECT id, user-  │◄──────────────────────┘
             │             │  name, password-   │
             │             │  hash FROM users   │
             └─────────────┤  WHERE username=?  │
                           └────────┬──────────┘
                                    │
                                    ▼
                           ┌───────────────────┐
                           │  Step ⑩ bcrypt.compare│
                           │                        │
                           │  compare(input, hash)  │
                           └────────┬───────────────┘
                                    │
                        ┌───────────┴───────────┐
                        │ 成功?                 │
                    ┌───┴───┐               ┌───┴───┐
                    │ YES   │               │ NO    │
                    └───┬───┘               └───┬───┘
                        │                       │
                        ▼                       ▼
                ┌───────────────┐      ┌───────────────┐
                │ Step ⑪ JWT    │      │ Step ⑫ 返回错误 │
                │     签发       │      │ 401 Unauthorized│
                │               │      │ "用户名或密码   │
                │ jwt.encode({  │      │  错误"          │
                │   sub: id,    │      └───────────────┘
                │   username    │
                │ }, SECRET,    │
                │  exp: 7d)     │
                └───────┬───────┘
                        │
                        ▼
                ┌───────────────┐
                │ Step ⑬ 返回响应│
                │               │
                │ {             │
                │   "access_    │
                │    token":"   │
                │    eyJ..."    │
                │ }             │
                └───────┬───────┘
                        │
                        ▼
                ┌───────────────────┐
                │ Step ⑭ 前端存储   │
                │                   │
                │ localStorage.setItem│
                │   ("token", token) │
                │ localStorage.setItem│
                │   ("user", user)   │
                └────────┬──────────┘
                         │
                         ▼
                ┌───────────────────┐
                │ Step ⑮ 跳转首页   │
                │                   │
                │ router.push("/")  │
                └────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │  Dashboard.vue 加载  │
              │                     │
              │  onMounted →        │
              │  GET /api/transac-  │
              │  tions?month=...    │
              │  GET /api/statistics│
              │   /monthly          │
              │  GET /api/statistics│
              │   /trend            │
              │                     │
              │  ✅ 显示收入/支出/  │
              │    结余统计卡片      │
              │  ✅ 渲染近6月趋势图  │
              │  ✅ 展示最近交易记录 │
              └─────────────────────┘
```

---

## 三、数据库数据流

```
                    ┌───────────────────────┐
                    │    SQLite Database     │
                    │    data/expenses.db    │
                    │                       │
                    │  ┌─────────────────┐  │
                    │  │  TABLE: users   │  │
                    │  │  ┌─┬───────┬───┐│  │
                    │  │  │id │username│  ││  │
                    │  │  ├─┼───────┼───┤│  │
                    │  │  │1 │zhangsan│hashed││
                    │  │  └─┴───────┴───┘│  │
                    │  └─────────────────┘  │
                    │                       │
                    │  ┌─────────────────┐  │
                    │  │TABLE: transactions││
                    │  │┌─┬───┬───┬───┐  ││
                    │  ││id│uid│type│amt││  │
                    │  ││─┼───┼───┼───┤  ││
                    │  ││1 │1 │exp │50 ││  │
                    │  ││2 │1 │inc │8000││  │
                    │  │└─┴───┴───┴───┘  ││
                    │  └─────────────────┘  │
                    └──────────┬────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │Auth Router  │    │TXN Router   │    │Stats Router  │
  │             │    │             │    │             │
  │INSERT       │    │INSERT       │    │SUM(type=inc) │
  │SELECT check │    │UPDATE       │    │SUM(type=exp) │
  │SELECT login │    │DELETE       │    │GROUP BY cat │
  └─────────────┘    └─────────────┘    └─────────────┘
```

---

## 四、关键模块交互关系

```
                    ┌─────────────────┐
                    │   vue-router    │
                    │                 │
                    │  beforeEach 守卫│
                    │  - requiresAuth │
                    │  - guest only   │
                    └────────┬────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │  api/index.js │  │  Login.vue   │  │Dashboard.vue │
  │              │  │              │  │              │
  │post login    │  │POST /api/    │  │GET /api/      │
  │post register │  │auth/login    │  │transactions   │
  │get user info │  │→ store token │  │GET /api/      │
  │get txns      │  │→ router.push │  │statistics/m-  │
  │create/put/del │  │ /dashboard  │  │onthly         │
  │txn           │  │              │  │GET /api/      │
  │              │  │              │  │statistics/    │
  │Axios拦截器:   │  │              │  │trend          │
  │Authorization │  │              │  └──────────────┘
  │Bearer token  │  │              │
  └──────────────┘  └──────────────┘
```

---

## 五、分类与颜色映射

| 类型 | 分类 | 图标 | 颜色 |
|------|------|------|------|
| 支出 | 餐饮 | 🍜 | #FF6B6B |
| 支出 | 交通 | 🚌 | #4ECDC4 |
| 支出 | 购物 | 🛍️ | #45B7D1 |
| 支出 | 娱乐 | 🎮 | #96CEB4 |
| 支出 | 房租 | 🏠 | #FFEAA7 |
| 支出 | 水电 | 💡 | #DDA0DD |
| 支出 | 医疗 | 💊 | #98D8C8 |
| 支出 | 教育 | 📚 | #F7DC6F |
| 收入 | 工资 | 💰 | #2ECC71 |
| 收入 | 奖金 | 🎁 | #27AE60 |
| 收入 | 兼职 | 🔧 | #1ABC9C |
| 收入 | 投资收益 | 📈 | #3498DB |
| 收入 | 红包 | 🧧 | #E74C3C |

---

*文档版本: v2.0.0 | 生成日期: 2026-07-26*
